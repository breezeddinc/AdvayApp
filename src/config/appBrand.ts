/**
 * ADVAY application branding.
 * Keep AT Protocol lexicon identifiers and @bsky.app package scopes unchanged;
 * those are protocol/dependency identifiers rather than application branding.
 */
export const APP_BRAND = {
  name: 'ADVAY',
  shortName: 'ADVAY',
  scheme: 'advay',
  bundleIdentifier: 'com.advay.app',
} as const
